<div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(<%= image_path('background.jpg')%>);">
  <div class="container">
    <h1>Save any kind of movies!</h1>
    <p>Superheros</p>
  </div>
</div>
<br>
<div class="container">
<h1>My Lists</h1>
<ul>
<% @lists.each do |list| %>
<li><%= link_to list.name, list_path(list) %> <%= link_to "delete", list_path(list), method: :delete, class: "btn btn-ghost"%></li>
<% end %>
</ul>
<%= link_to "Create new List", new_list_path, class: "btn btn-primary" %>
</div>
